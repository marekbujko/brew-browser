# 2026-05-26 — v0.4.0 ship (velocity + opt-in history endpoint)

**Phase:** v0.4.0 ship — Trending Velocity (A) + Opt-in History Endpoint (B)
**Status:** ⏳ Steps 1–8 of 9 complete on branch. Step 9 (Caddy deploy on `brew-browser.zerologic.com`) is the only remaining piece before merge.
**Branch:** `feat/v0.4.0-velocity-and-history` (off `main` at `d6d28a0`)
**Workflow change (durable):** from this branch onward, all merges to `main` go through pull requests rather than direct pushes.

## Scope

A package's 30-day install count tells you it's `ca-certificates`. Velocity tells you what's actually getting adopted right now. v0.4.0 introduces:

- **Velocity scoring (A, free)** — derive an "above/below annual monthly average" index from the three rolling windows formulae.brew.sh already publishes. Sort by velocity by default; the heat (🔥) and cool (❄️) badges replace the "every row is ca-certificates" leaderboard.
- **`install-on-request` switch** — pull the secondary endpoint in parallel with the primary `install` endpoint and surface the user-initiated install count. Strips dependency-pull noise from the signal.
- **Opt-in history endpoint (B)** — `brew-browser.zerologic.com/trending-history/*`. Distinct trust boundary (our infra vs. Homebrew first-party), so gated by a new `enhanced_trending_enabled` toggle in Settings → Network. Paranoid mode hard-locks it off.
- **Seed-from-rolling-windows trick** — the day the collector turns on, it derives three historical buckets per package from current 30d/90d/365d subtractions. Charts have data on day 0; granularity grows from daily snapshots thereafter.

This task record covers the backend pieces; frontend, collector, docs, and Caddy come in their own commits / task records.

## Decisions

- **D1 — Endpoint domain:** `brew-browser.zerologic.com/trending-history/*` (subpath on existing vhost), not a new `trending-history.zerologic.com`. Reuses the Caddy + cert already serving the updater manifest. Same DNS-level trust boundary; only feature-level differentiation.
- **D2 — GitHub mirror of nightly JSON:** deferred to v0.5+. The auditability is nice-to-have, not blocking.
- **D3 — Sort default:** velocity desc, with an inline mini-sparkline per row when enhanced trending is on (mirrors star-history.com list view). Index blob carries the compact sparkline so the trending list renders from one fetch.
- **D4 — Sparkline empty state when toggle is off:** passive — only present in Settings → Network. No placeholder, no banner. Detail panel simply omits the sparkline area when the toggle is off.
- **D5 — Velocity computation:** server-side in `trending::client::fetch` → joined in `commands::trending::trending_fetch` from the cached 30d/90d/365d. Frontend never needs to know the formula.

## What landed (backend Steps 1–3)

### Step 1 — Settings + per-feature gate + new error variant

- `src-tauri/src/commands/settings.rs:127` — new `enhanced_trending_enabled: bool` field. `#[serde(default)]`, defaults to `false` in `Settings::default()`. Forward-compat tested: v0.3.x settings.json missing this field reads as `false` (opt-in posture preserved).
- `src-tauri/src/state.rs:269` — new `require_enhanced_trending()` helper that composes the master `require_network` gate with the per-feature toggle. Returns `ParanoidModeBlocked` when paranoid wins, `FeatureDisabled` when only the toggle is off. FirstLaunch → `FeatureDisabled` (no zerologic.com traffic until explicit opt-in).
- `src-tauri/src/error.rs:73` — new `BrewError::FeatureDisabled { feature: String }` variant. Distinct from `ParanoidModeBlocked` so frontend toast routes to the right setting (per-feature toggle vs. master Offline Mode switch).

**Tests (+9):**
- `error::tests::feature_disabled_serializes_with_feature`
- `commands::settings::tests::enhanced_trending_defaults_to_false`
- `commands::settings::tests::missing_enhanced_trending_field_defaults_to_false`
- `commands::settings::tests::enhanced_trending_round_trips_with_camel_case_key`
- `state::tests::require_enhanced_trending_allows_when_toggle_on_and_paranoid_off`
- `state::tests::require_enhanced_trending_blocks_when_toggle_off`
- `state::tests::require_enhanced_trending_blocks_when_paranoid_on_even_if_toggle_on`
- `state::tests::require_enhanced_trending_blocks_on_first_launch`
- `state::tests::require_enhanced_trending_blocks_when_corrupt`

### Step 2 — install-on-request fetch + velocity computation

- `src-tauri/src/trending/client.rs:12` — split `HOST` constant into `HOST_INSTALL` + `HOST_INSTALL_ON_REQUEST`. Refactored `fetch` to call both via `tokio::join!` and merge results. Soft-fails the secondary fetch (entries still ship with `install_on_request_count: None` if it errors).
- `src-tauri/src/trending/client.rs:140` — extracted `merge_entries(install_items, ior_map, installed)` as a pure testable helper. Sort + re-rank + truncate logic moved out of the async fetch path so it can be unit-tested directly.
- `src-tauri/src/trending/velocity.rs` (NEW) — `velocity_index(c30, c90, c365) → Option<f64>`. Pure function. Returns `None` on degenerate inputs (zero annual count, non-monotonic windows, too-small absolute numbers).
- `src-tauri/src/commands/trending.rs:46` — `trending_fetch` now eager-warms all three windows for the requested call, fanning out via `tokio::task::JoinSet`. Velocity is back-filled from the cross-window map. Subsequent calls within TTL hit the cache for all three windows; no network. Graceful per-window soft-fail.
- `src-tauri/src/types.rs:340` — extended `TrendingEntry` with optional `install_on_request_count` + `install_on_request_count_formatted` + `velocity_index`. All `#[serde(skip_serializing_if = "Option::is_none")]` so the wire shape stays back-compat with v0.3.x clients (no frontend update required to keep existing functionality).

**Tests (+14):**
- 7 in `trending::velocity::tests` (heating, cooling, steady, zero, non-monotonic, too-few-installs, typical)
- 7 in `trending::client::tests::merge_*` (both-present, IOR-missing degraded path, IOR-only-package dropped, sort+re-rank, truncate-to-max, velocity-starts-none, installed-marker)

### Step 3 — trending_history IPC + client + LRU cache

- `src-tauri/src/trending/history/` (NEW directory):
  - `mod.rs` — header docstring describing the opt-in trust boundary.
  - `client.rs` — `fetch_index()` + `fetch_package(name, kind)` against `brew-browser.zerologic.com/trending-history/*`. URL builder validates the package token against a strict whitelist (`[A-Za-z0-9._+@-]`) to prevent path traversal and refuse bogus inputs before the round-trip.
  - `cache.rs` — `TrendingHistoryCache` with `CachedIndex` single-slot + `CachedSeries` per-package LRU. TTL 6 hours (collector regenerates nightly). Cap 500 per-package entries with oldest-by-fetched_at eviction.
- `src-tauri/src/types.rs:367` — five new types:
  - `TrendingHistorySource { Seed, Daily }`
  - `TrendingHistoryPoint { date, count_30d?, count_90d?, count_365d?, count_install_on_request_30d?, estimated_daily_installs?, source }`
  - `TrendingHistorySeries { name, kind, points, generated_at, cache_age_seconds }`
  - `TrendingHistoryIndex { generated_at, packages, cache_age_seconds }`
  - `TrendingHistoryIndexEntry { name, kind, velocity_index?, sparkline: Vec<u64> }`
- `src-tauri/src/state.rs` — added `trending_history_cache: Arc<Mutex<TrendingHistoryCache>>` field to `AppState`. Initialized empty at startup.
- `src-tauri/src/commands/trending.rs:228` — two new IPCs:
  - `trending_history_index() → TrendingHistoryIndex` (gated by `require_enhanced_trending`)
  - `trending_history_fetch(name, kind) → TrendingHistorySeries` (same gate, plus path-traversal guard in the client)
  - Both follow the same cache-hit / fetch / stale-fallback contract as the existing `trending_fetch`.
- `src-tauri/src/lib.rs:132` — registered both new IPCs in `tauri::generate_handler![]` via the existing `commands::*` glob re-export at `commands/mod.rs:42`.

**Tests (+10):**
- 3 in `trending::history::client::tests` (rejects empty name, rejects path traversal, accepts legitimate brew tokens including `@`/`+`/`.`/`_`)
- 7 in `trending::history::cache::tests` (ttl-is-six-hours, empty-returns-none, put-then-get index, put-then-get series, replace-existing, evict-oldest-at-cap, clear-evicts-all)

## Tests & lint at backend checkpoint

- `cargo test`: **506 passed**, 0 failed, 6 ignored (473 → 506, +33 new)
- `cargo build`: clean (zero dead-code warnings — `FeatureDisabled` and `require_enhanced_trending` are now actually used)
- No frontend changes yet; `npm run check` unchanged from v0.3.1.

## Wire-shape invariants pinned by tests

| Invariant | Test |
|---|---|
| Toggle off → `FeatureDisabled` (not `ParanoidModeBlocked`) | `require_enhanced_trending_blocks_when_toggle_off` |
| Paranoid on, toggle on → `ParanoidModeBlocked` (master switch wins) | `require_enhanced_trending_blocks_when_paranoid_on_even_if_toggle_on` |
| FirstLaunch → `FeatureDisabled` (no zerologic.com traffic until opt-in) | `require_enhanced_trending_blocks_on_first_launch` |
| Corrupt settings → `ParanoidModeBlocked` (fail-closed via inner gate) | `require_enhanced_trending_blocks_when_corrupt` |
| `enhancedTrendingEnabled` serializes camelCase on disk | `enhanced_trending_round_trips_with_camel_case_key` |
| Missing field defaults false (forward-compat for v0.3.x → v0.4.0 upgrade) | `missing_enhanced_trending_field_defaults_to_false` |
| URL builder rejects path traversal (`..`, `/`, spaces, `;`) | `fetch_package_rejects_path_traversal` |
| Velocity returns `None` for degenerate / tiny-sample inputs | `returns_none_on_zero_annual_count`, `returns_none_on_non_monotonic_windows`, `returns_none_on_too_few_installs` |

## Files changed (backend only — frontend/collector still ahead)

```
 src-tauri/src/commands/settings.rs        |  97 +++++++
 src-tauri/src/commands/trending.rs        | 283 ++++++++++++++++--
 src-tauri/src/error.rs                    |  19 ++
 src-tauri/src/lib.rs                      |   2 +
 src-tauri/src/state.rs                    | 121 ++++++++
 src-tauri/src/trending/client.rs          | 300 ++++++++++++++++---
 src-tauri/src/trending/mod.rs             |   6 +-
 src-tauri/src/trending/velocity.rs        |   NEW (~115 lines)
 src-tauri/src/trending/history/mod.rs     |   NEW (~22 lines)
 src-tauri/src/trending/history/client.rs  |   NEW (~150 lines)
 src-tauri/src/trending/history/cache.rs   |   NEW (~225 lines)
 src-tauri/src/types.rs                    | 114 ++++++++
 8 modified, 4 new
```

## Step 4 — Settings → Network UI + opt-in subsection (commit `6711133`)

- `src/lib/types.ts:484` — `Settings.enhancedTrendingEnabled: boolean` added to interface + `SETTINGS_DEFAULTS`. New `feature_disabled` variant in `BrewErrorPayload` union with friendly-message routing to "Settings → Network."
- `src/lib/api.ts:248` — new `trendingHistoryIndex()` + `trendingHistoryFetch(name, kind)` IPC bindings.
- `src/lib/components/SettingsSectionTrendingHistory.svelte` (NEW, ~170 lines) — opt-in subsection modeled on `SettingsSectionUpdates.svelte`. Toggle disabled with "locked off" hint when Offline Mode is on. Hint copy enumerates the data practice in plain language (only package name sent, no IP logged, no cookies, no fingerprinting).
- `src/lib/components/SettingsSectionNetwork.svelte` — 6th `pathStatuses` entry for `brew-browser.zerologic.com/trending-history` (allowed only when toggle ON AND paranoid OFF). New `<SettingsSectionTrendingHistory />` mounted alongside the existing `<SettingsSectionUpdates />`.

## Step 5 — Trending tab: velocity column + inline sparklines (commit `6711133`)

- `src/lib/components/Trending.svelte` — default sort key changed from `rank` (asc) to `velocity` (desc). New Velocity column with `Flame`/`Snowflake`/dash badge + numeric value, tier-coded (surge `>=1.5` warm, cool `<=0.5` blue, neutral plain). New `velocityOf(name, kind, fallback)` helper prefers the history-index value over the backend's per-fetch computation. None-velocity entries sort last regardless of direction so missing data never wins the leaderboard. Count cell becomes vertical-flex with the formatted number on top and an inline `TrendingSparkline` beneath (when `enhancedReady` derived true). Responsive grid reworked from 7 cols to 8.
- `src/lib/components/TrendingSparkline.svelte` (NEW) — shared SVG sparkline with `inline` (60×16 stroke-only) and `detail` (360×80 with current-dot) variants. Auto-fits min/max scaling so trajectory reads regardless of magnitude. Renders nothing gracefully when data is empty, all-zero, or single-point.
- `src/lib/stores/trendingHistory.svelte.ts` (NEW, ~150 lines) — index slot + per-package series Map. `enabled` getter is single source of truth for toggle-on AND paranoid-off. `ensureIndexLoaded()`, `ensureSeriesLoaded(name, kind)` are idempotent and silent-on-failure (feature is enrichment, not load-bearing). Sync lookups `entryFor` / `sparklineFor` / `velocityFor` / `seriesFor`.

## Step 6 — PackageDetail sparkline (commit `6711133`)

- `src/lib/components/PackageDetail.svelte` — fires `trendingHistory.ensureSeriesLoaded(name, kind)` from `loadDetail` (the store's `enabled` getter makes this safe to call unconditionally). New `trend-card` section between description and AI-enriched blocks renders the `detail`-variant `TrendingSparkline`. Sub-header label distinguishes seed-only history ("Bootstrap + daily snapshots — granularity grows over time") from real daily history ("Daily install snapshots"). Strictly **passive** per D4: no placeholder when the toggle is off; the section simply doesn't exist.

## Step 7 — Collector for brew-browser.zerologic.com (commit `6901b64`)

- `tools/trending-collector/` (NEW directory, 7 files, 913 lines):
  - `package.json` — Node 20+ ESM, single dependency `better-sqlite3@^11.5.0`.
  - `lib/common.js` — SQLite schema (composite-PK `snapshots` table, WAL journaling), HTTP helpers, `parseCount`/`extractItems` mirroring the Rust client's defensive deserialization, `velocityIndex` math byte-for-byte mirror of the Rust version, atomic JSON writes (`.tmp` + `rename` so Caddy never serves half-written files), `isSafePackageToken` validator.
  - `lib/render.js` — regenerates `out/index.json` (top-500 by velocity with ~30-day inline sparklines) and `out/<kind>/<name>.json` (full `TrendingHistorySeries`). Per-day install estimate via adjacent-day `count_30d` subtraction (only when both snapshots are `daily` source).
  - `seed.js` — one-shot bootstrap. Derives three historical buckets per package via rolling-window subtraction (c30, c90-c30, c365-c90) tagged `source='seed'`. ALSO writes today's c30/c90/c365 as `source='daily'` so the next nightly run has a predecessor to subtract from. Refuses to re-seed unless `snapshots` table is dropped.
  - `collect.js` — nightly entrypoint. 12 concurrent HTTP GETs (4 categories × 3 windows), `INSERT OR IGNORE` keyed by PK so same-day re-runs are no-ops, regenerate JSON output. Idempotent and soft-fails per-endpoint.
  - `README.md` — deploy walkthrough (rsync code → `npm ci` on box → run `seed.js` once → add cron line → configure Caddy block referencing security.md §16). Layout: `/home/michael/Sites/brew-trending-collector/` (code), `/home/michael/data/brew-trending/db.sqlite` (state), `/home/michael/Sites/brew-trending/` (Caddy-served JSON output).
  - `.gitignore` — `node_modules/`, `state/`, `out/`.

## Step 8 — Memory bank + docs polish (this commit)

- `memory-bank/projectbrief.md:35` — "nine documented paths" → "ten documented paths"; new item (j) for `brew-browser.zerologic.com/trending-history/*`.
- `memory-bank/decisions.md` — new ADR `2026-05-26: Opt-in trust boundary for enhanced trending history (v0.4.0)`. Documents the four rejected alternatives + the rationale for the per-feature gate vs master-switch-only.
- `memory-bank/security.md` §16 — full endpoint audit. Includes the actual Caddyfile snippet (IP-redaction via `request>remote_ip "0.0.0.0"`, GET-only via `respond @writes 405`, `-Set-Cookie` header strip, 6h cache-control). Threat-model table covers MITM, compromised endpoint, log misconfiguration, forgotten-toggle, bug-in-gate, future-contributor-adds-path scenarios. Pre-launch checklist for Step 9 deploy verification.
- `memory-bank/techContext.md` — Trending data sources section rewritten to cover both the always-on Homebrew first-party endpoints + the opt-in zerologic.com endpoint; collector + seed-trick explained.
- `memory-bank/backendApi.md` §13.14 — new section covering v0.4.0 backend surface: `enhanced_trending_enabled` setting field, `require_enhanced_trending` gate, `FeatureDisabled` error variant, extended `TrendingEntry` wire shape, `trending_history_index` + `trending_history_fetch` IPCs, per-package LRU cache, server-side velocity math, +33 tests pinning the security invariants.
- `memory-bank/frontendComponents.md` — new v0.4.0 additions block covering `TrendingSparkline`, `SettingsSectionTrendingHistory`, Trending tab restructure (velocity column, sort default change, inline sparklines, 8-col responsive grid), PackageDetail integration, new `trendingHistory.svelte.ts` store.
- `docs/release-notes/0.4.0.md` (NEW) — user-facing release notes covering velocity scoring, `install-on-request` signal, opt-in Enhanced Trending History endpoint, seed-trick, Settings → Network reorganization, what stays unchanged for users who don't opt in, under-the-hood details, acknowledgements.
- `README.md` — outbound-paths disclosure: nine → ten. Path (a) rewritten to mention `install-on-request` and the cross-window velocity join. New path (j) entry for the opt-in endpoint with the trust-boundary callout.

## Step 9 — Caddy privacy hardening + verification (NOT YET DONE)

The actual `caddy reload` on `brew-browser.zerologic.com` and the pre-launch curl-verification checklist from `security.md` §16.6 still need to happen. Then the branch can PR into main and v0.4.0 can ship.

The Caddy config that needs to land is documented verbatim in `security.md` §16.2 — anyone with ssh access to the box can copy it. The pre-launch checklist:

- [ ] Caddy block deployed and `caddy reload`'d on `brew-browser.zerologic.com`
- [ ] `curl -I https://brew-browser.zerologic.com/trending-history/index.json` returns `Cache-Control: public, max-age=21600` and **no `Set-Cookie`**
- [ ] `curl -X POST` returns `405 Method Not Allowed`
- [ ] Tail of `/var/log/caddy/brew-trending.log` shows `"remote_ip":"0.0.0.0"` for the test request above
- [ ] `tools/trending-collector/seed.js` run once and `index.json` lands at the URL with non-empty `packages`
- [ ] Frontend with `enhancedTrendingEnabled = true` renders inline sparklines on the Trending tab

## Workflow note: PRs from here on

Going forward, all merges to `main` go through pull requests. This branch will be pushed and then PR'd in; same for every future branch. No more direct pushes to `main`.

## References

- Plan in conversation: full file:line citations + 5 decisions + 4-layer architecture diagram + risks table + rollout calendar.
- Cross-session memory: `~/.claude/projects/-Users-michael-Clean/memory/feedback_principled_fixes.md` (informed the "ship the proper architectural fix" framing).
- Inspired by [star-history.com](https://star-history.com) — list view with inline sparklines (D3).
- Homebrew analytics API docs: <https://docs.brew.sh/Analytics> (confirms only 30d/90d/365d windows are public; no time-series endpoint).
